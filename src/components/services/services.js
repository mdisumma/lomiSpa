import "./services.css";

function Services(props) {
	return (
		<div>
			{/* {props.items.map((service) => {
				<div key={service._id}>
					<h2>{service.name}</h2>
					<p>{service.description}</p>
				</div>;
			})} */}
		</div>
	);
}

export default Services;
